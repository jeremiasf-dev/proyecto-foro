# Stack:
	# C# / .NET 		(Backend) (Mi lenguaje principal, potente y permite unificar foro y chat)
	# HTML/CSS y JS mínimo  (Frontend)(Lo básico en estructura y diseño, y lo mínimo en dimamicidad)
	# PostgreSQL o MySQL    (Persistencia, a definir) (No hay motivo mas porque al primero lo sé usar, y al segundo lo estudiaré este año)
	

# Host:
	# Fly.io al principio, hasta que pueda sostener un host y dominio, posiblemente el servidor sea local

# A futuro
	# Ya que estoy desarrollando un motor gráfico, cuando el foro respire podría agregar videojuegos lo más relajantes posibles.


